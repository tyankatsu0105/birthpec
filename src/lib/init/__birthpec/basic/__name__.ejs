---
to: <%= filePath %>/__tests__/<%= name %>.spec.ts
---
import {<%= exports %>} from '<%= name %>';

describe('<%= name %>', () => {
  <% for (const item of exports) { %>
    describe('<%= item %>', () => {
      it('', () => {
        
      });
    });
  <% } %>
});